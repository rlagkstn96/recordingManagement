import 'package:flutter/material.dart';

class Setting_Setting extends StatelessWidget {
  const Setting_Setting({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.yellow,
    );
  }
}
