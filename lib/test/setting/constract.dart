import 'package:flutter/material.dart';

class Setting_Constract extends StatelessWidget {
  const Setting_Constract({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.orangeAccent,
    );
  }
}
