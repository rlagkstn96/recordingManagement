import 'package:flutter/material.dart';

class Setting_Notice extends StatelessWidget {
  const Setting_Notice({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green,
    );
  }
}
