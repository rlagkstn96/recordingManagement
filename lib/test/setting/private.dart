import 'package:flutter/material.dart';

class Setting_Private extends StatelessWidget {
  const Setting_Private({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.red,
    );
  }
}
